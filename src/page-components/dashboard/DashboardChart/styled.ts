import styled from '@emotion/styled';
import { darkGray } from 'src/colors';

export const Wrapper = styled.div`
  border: 1px solid ${darkGray};
  box-sizing: border-box;
  margin: 0.25rem;
  padding: 0.5rem;
`;