import styled from '@emotion/styled';
import { blue, white } from 'src/colors';

export const WidgetTitle = styled.div`
  background: ${blue};
  color: ${white};
  font-size: .7rem;
  padding: 0.5rem;
`;
